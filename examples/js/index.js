window.onload = function () {
    // Just provide selector to the elements you want to use as range sliders
    // Or just use the universal selector to transform all input[type='range'] in the page into SJ slider
    const SJRanges = new SJ.init("*");
}